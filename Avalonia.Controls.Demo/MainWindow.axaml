<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="250"
        xmlns:ext="using:Avalonia.Controls.Extensions"
        x:Class="Avalonia.Controls.Demo.MainWindow"
        Title="Avalonia.Controls.Demo">
  <StackPanel Orientation="Vertical">
    <TextBlock Text="{Binding Message}" Foreground="Red"/>
    <ext:HorizontalItemsRepeater Items="{Binding Items}" Command="{Binding OnButtonClick}" Spacing="20">
      <ItemsRepeater.ItemTemplate>
        <DataTemplate>
          <ext:ClickablePanel CommandParameter="{Binding content}">
            <TextBlock Text="{Binding content}" />
          </ext:ClickablePanel>
        </DataTemplate>
      </ItemsRepeater.ItemTemplate>
    </ext:HorizontalItemsRepeater>
    <ext:GridView ColumnNum="4" Items="{Binding Items}">
      <ext:GridView.ItemTemplate>
        <DataTemplate>
          <ext:ClickablePanel CommandParameter="{Binding content}">
            <TextBlock Text="12345678" />
            <TextBlock Text="{Binding content}" />
          </ext:ClickablePanel>
        </DataTemplate>
      </ext:GridView.ItemTemplate>
    </ext:GridView>
  </StackPanel>
</Window>